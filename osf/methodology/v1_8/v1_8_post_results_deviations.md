# AIAS™ v1.8 — Post-Results Deviations Log

Running deviations log for v1.8, **post-results**. Entries 0–3 are sealed inside the
pre-registration content module and deposited at `v1.8-prereg-r4` / `v1.8-results-r1`
(commit `85d2202`); they are immutable. Entries here are **additive and post-results** —
they record workflow/deliverable decisions taken after the results lock and do **not**
modify the sealed module or force-move any tag (`v1.8-prereg-r1..r4`, `v1.8-results-r1`
all unchanged). This file deposits alongside the results bundle at the methodology
namespace.

---

## Entry 4 (post-results; external to the sealed module) — deliverable-scope change

Locked metadata carried `register: academic` / "report deferred to AIAS 2.0," an
inherited scaffold default. A methodological brand-format report is produced at v1.8
instead — additive; no verdict, threshold, or hypothesis affected; follows v1.7
precedent (`reports/v1_7_cpc_consistency_content.py`, a fully-authored methodology-track
report under the same `register: academic`). Recorded post-results.

Scope of the report is **methodological** (the redesigned-and-validated instrument), not a
brand-standings leaderboard: v1.8 re-scored a frozen omnibus with no new acquisition, so
presenting the re-scored brands as fresh standings would overstate the phase and overlap
the original category phases. Concrete brands appear only as COI-safe illustrations of what
the instrument now reveals — the nine φ/J-discordant units and the twenty-nine low-recall
units the prior instrument could not score — not as rankings; no Samsung-affiliated brand is
spotlighted. The sealed module, its DEVIATIONS Entries 0–3, and all tags are unchanged.

*(Secondary / optional, not done here: a one-line backfill on the v1.7 record noting the same
deferred-note-vs-report departure, for parity. Left to the v1.7 lineage's own maintenance.)*

---

## Entry 5 — OSF deposit completed post-submission (deposit/publication order inverted for 6963359)

**Context.** The v1.8 working paper was submitted to SSRN and distributed as abstract **6963359** outside the tracked build/deposit workflow. At the time of that submission the OSF deposit of this phase's post-results materials (paper, scorer, deviations log) had not been run; the project archive held only the results-lock artifacts — pre-registration, verdicts, instrument table, and materialization manifest. The submitted paper's "deposited at OSF" statements (Abstract; Declarations §Data and materials, which names the scorer specifically) therefore preceded the deposit they assert.

**Action.** This deposit is completed **after** public submission, not before. The deposited paper is the same build distributed as 6963359 (deposited = published). The standing program rule is that the external anchor/deposit precedes the one-way publication boundary; that order was not met for 6963359, and this entry records the true sequence rather than representing the deposit as having preceded submission.

**Scope guarantee.** No analysis, value, threshold, or verdict changed. The sealed results-lock (commit 85d2202, tag v1.8-results-r1) is untouched; this entry is additive and external to the sealed module. The pre-registration tags are not moved.

**Deposit date:** 2026-06-18
