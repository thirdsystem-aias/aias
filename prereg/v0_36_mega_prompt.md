# AIAS™ v0.36 — Mega-Prompt: NOT APPLICABLE (No-Acquisition Re-Analysis)

**Phase:** v0.36 — CPC Regime Emergence
**Lock tag:** v0.36-prereg-r1

This phase issues **no LLM acquisition calls**. It is a fully retrospective
re-analysis (v0.33 / v0.35 pattern) of frozen quantities:

- Per-brand 6-dim per-model CPC vector (v1.7 computation; characterization
  quantity, not adopted), z-scored within substrate per model
- Per-brand canonical Presence composite (from phase verdicts) — used for the
  residualization arm
- Locked per-brand Four-Regime assignments (taxonomy per Protocol v1.2,
  SSRN 6761698) — the cluster-concordance target

Primary set: v0.31 retroactive CV-CPC cross-category baseline, sourced from the
v0.31 OSF deposit (osf.io/ec6wh/v31/) as archived data only — resolves to the
5-substrate panel-uniform omnibus v0.19 / v0.20 / v0.21 / v0.22 / v0.23,
canonical 6-model panel. Robustness set: v0.35 frozen 84-unit analysis set
(verdict-concordance check only).

No prompts are defined for this phase. There is **no brand registry** this
phase; the analysis sets are enumerated in the locked content module.

Design, feature space, clustering procedure, hypotheses, criteria, verdict
matrix, predictions, guardrails: see
`prereg/v0_36_cpc_regime_emergence_content.py` (the locked artifact).
