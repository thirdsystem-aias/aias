# v0.7 Phase 2 BBB — Phantom Brand Persistence build pipeline

Three files that drop into your existing `/Users/pablou/aias/reports/` directory and produce the v0.7 Phase 2 PDF using the same architecture as `build_report.py` (v0.6 cross-category).

## Files in this delivery

```
build_report_v07.py      ← copy of build_report.py with v0.7-specific edits
v07_bbb_content.py       ← Phase 2 BBB content (mirrors v06_content.py structure)
build_charts_v07_bbb.py  ← matplotlib chart generator (5 figures at locked figsize)
```

## Installation

Drop all three files into `/Users/pablou/aias/reports/` alongside the existing v0.6 build files. **No edits to `build_report.py` or `v06_content.py`.** v0.6 keeps building exactly as it does today.

```
/Users/pablou/aias/reports/
├── build_report.py             (v0.6 — untouched)
├── v06_content.py              (v0.6 — untouched)
├── build_report_v07.py         ← NEW
├── v07_bbb_content.py          ← NEW
├── build_charts_v07_bbb.py     ← NEW
├── tsboilerplate.py            (existing — both builds use it)
├── venv/                       (existing)
└── output/
    ├── chart_*.pdf             (v0.6 charts — leave alone)
    ├── chart_v07_*.pdf         ← NEW (generated by build_charts_v07_bbb.py)
    ├── v06_cross_category.pdf  (v0.6 output — leave alone)
    └── v07_phantom_persistence.pdf  ← NEW (generated by build_report_v07.py)
```

## Build

```bash
cd ~/aias/reports
source venv/bin/activate

python3 build_charts_v07_bbb.py
python3 build_report_v07.py
```

Output: `~/aias/reports/output/v07_phantom_persistence.pdf` — 14 pages.

## What's identical to the v0.6 pipeline

- ReportLab two-pass build (Pass 1 = layout with chart reservations, Pass 2 = pypdf overlays charts)
- 6-column grid, 81.667pt columns, 10pt gutter, 540pt content width
- Cover, body (3+3 split), spread (single 540pt frame), closing page templates
- Akkurat Pro auto-detection from `~/.fonts/Akkurat`, `~/Library/Fonts`, etc.
- Indigo `#37237B` primary, soft black `#231F20` secondary, paper `#FAF7F2` background
- Header (wordmark left + version label right), footer (verbatim copyright + page number)
- All chart figsize specs from `chart_construction_rules` — charts rendered at native size, no rescaling

## What changed for v0.7

| Change | v0.6 | v0.7 |
| --- | --- | --- |
| Section label | `PATTERN N` | `FINDING 0N` |
| Header right text | `Cross-Category Findings · v0.6 · April 2026` | `Phantom Brand Persistence · v0.7 · 4 May 2026` |
| Findings count | 6 | 5 (4 with charts, 1 text-only) |
| Three modes section | included | **omitted** (skipped by `hasattr(content, "THREE_MODES")` guard) |
| Aggregate matrix page | included | **replaced** by Hypothesis Scoring table page |
| Output filename | `v06_cross_category.pdf` | `v07_phantom_persistence.pdf` |
| Citation slug | `thirdsystem.ai/v06-cross-category` | `thirdsystem.ai/v07-phantom-persistence` |

## Charts

`build_charts_v07_bbb.py` produces five PDFs at locked figsize:

| File | Figsize | Slot key in build_report_v07 |
| --- | --- | --- |
| `chart_v07_leaderboard_6col.pdf` | 7.50 × 7.50 (6_col_hero_xl) | `hero_leaderboards` |
| `chart_v07_f1_comparator_6col.pdf` | 7.50 × 5.00 (6_col_hero) | `hero_f1_comparator` |
| `chart_v07_f2_valence_6col.pdf` | 7.50 × 5.00 (6_col_hero) | `hero_f2_valence` |
| `chart_v07_f3_freshness_4col.pdf` | 3.68 × 2.85 (3_col_inline) | `inline_f3_freshness` |
| `chart_v07_f4_temporal_6col.pdf` | 7.50 × 5.00 (6_col_hero) | `hero_f4_temporal` |

The chart generator pulls the Indigo color from `brand/third_system_brand.json`. Auto-detects Akkurat Pro the same way `build_report.py` does — falls back to Inter, then DejaVu Sans with a warning. Charts use **`pdf.fonttype = 3`** (Type 3 vector outlines) instead of TrueType so there are no font-name collisions when pypdf overlays them on the ReportLab base PDF — important for clean text rendering after merge.

## Sandbox notes (informational only)

I built this in a Linux sandbox without Akkurat Pro, using Inter as fallback. The sandbox build produced a 14-page PDF that renders cleanly — all charts placed correctly, all 5 chart reservations matched their files, hypothesis scoring table fits the spread frame. On your Mac with Akkurat Pro the output will use real Akkurat throughout (cover title, body text, charts, hypothesis table) instead of Inter.

## Section structure in the final PDF

```
Page  1   Cover (title, subtitle, byline, tagline)
Page  2   Standfirst + lead deck + executive summary (10 paragraphs)
Page  3   Household goods leaderboard (hero spread)
Page  4   What we measured (methodology, 6 paragraphs)
Page  5   Finding 01: The phantom replicates
Page  6   Finding 01 chart (BBB vs Pier 1 comparator) + Finding 02 start
Page  7   Finding 02: The phantom is not a knowledge gap. It is a recommendation slot.
          + Finding 02 chart (per-model valence)
Page  8   Finding 03: Newer models phantom-mention better, not less
          + Finding 03 chart (within-lab freshness, inline)
          + Finding 04 start
Page  9   Finding 04: Different prompts activate different temporal frames
          + Finding 04 chart (per-CEP temporal frames)
Page 10   Finding 05: The rebrand has not propagated. The legacy brand has.
          (text-only, no chart)
Page 11   Limitations (7 paragraphs)
Page 12   What's next (6 paragraphs)
Page 13   Hypothesis scoring (table, 8 rows)
Page 14   About the Third System / methodology / citation / datasets
```

## Iteration loop

If you want to revise content (a paragraph, a finding, the executive summary), edit `v07_bbb_content.py` only and re-run `python3 build_report_v07.py`. Charts don't need regenerating unless data changes — they're decoupled from text via the slot-name lookup table in `build_report_v07.py:_slot_lookup()`.

If you want to revise a chart (add a brand, change a number), edit `build_charts_v07_bbb.py` and re-run both scripts in sequence (charts first, then report).

If a chart's locked figsize changes, also update `_slot_lookup()` in `build_report_v07.py` to point at the new figsize key.
